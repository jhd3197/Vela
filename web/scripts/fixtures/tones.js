// The tones the primitive layer knows, in a plain module so the fixture page
// and the suite that reads it agree on the list without one of them importing
// JSX that Node cannot load.
export const TONES = ['accent', 'neutral', 'cyan', 'green', 'amber', 'red'];
