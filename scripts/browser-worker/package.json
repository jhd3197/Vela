{
  "name": "vela-browser-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Private supervised Node worker that runs an agent desktop's managed Chromium session. Vela starts and stops it; it has no network listener and no Vela credentials.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.11.0"
  },
  "dependencies": {
    "playwright-core": "1.63.0"
  },
  "scripts": {
    "test": "node --test test"
  }
}
