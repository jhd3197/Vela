# Claude instructions

@AGENTS.md

Follow the shared repository guidance in [AGENTS.md](AGENTS.md), including the
required `CHANGELOG.md` update for user-facing and contributor-facing changes.
Keep implementation plans in gitignored `plans/`, and current public guidance
in `docs/`. This file imports the shared rules so they have one source of truth.
